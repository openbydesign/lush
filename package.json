{
  "name": "lush",
  "version": "0.3.1",
  "private": true,
  "packageManager": "bun@1.3.8",
  "scripts": {
    "dev": "bun --bun scripts/dev/index.ts",
    "app:dev": "bun run --cwd apps/lush dev",
    "api:dev": "bun run --cwd services/api dev",
    "agent:dev": "bun run --cwd services/agent dev",
    "code:canary": "bun scripts/code-canary.ts",
    "docs:dev": "bun run --cwd services/docs dev",
    "open:web": "open http://localhost:5874",
    "open:docs": "open http://localhost:7332",
    "open:app": "open apps/lush/src-tauri/target/release/bundle/macos/Lush.app",
    "db:up": "docker compose up -d lush-postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f lush-postgres",
    "db:migrate": "bun run --cwd packages/db migrate",
    "auth:keygen": "bun run --cwd services/authz keygen",
    "auth:verify-email": "bun run --cwd services/authz verify-email",
    "api:codegen": "bun run --cwd services/api codegen",
    "api:openapi": "bun run --cwd services/api openapi:codegen",
    "typecheck": "bun run api:codegen && bun run api:openapi && bun run --cwd packages/config typecheck && bun run --cwd packages/code typecheck && bun run --cwd packages/logging typecheck && bun run --cwd packages/db typecheck && bun run --cwd services/notifications typecheck && bun run --cwd services/authz typecheck && bun run --cwd services/inference typecheck && bun run --cwd services/sessions typecheck && bun run --cwd services/agent typecheck && bun run --cwd services/api typecheck && bun run --cwd packages/api-client typecheck && bun run --cwd apps/lush typecheck && bun run --cwd services/docs typecheck",
    "test:repo": "bun run typecheck && bun build services/api/src/server.ts --target bun --outdir /tmp/lush-api-check && bun build services/agent/src/server.ts --target bun --outdir /tmp/lush-agent-check",
    "test": "bun test",
    "build": "bun run typecheck && bun run --cwd apps/lush bundle",
    "tauri:dev": "bun run --cwd apps/lush tauri:dev",
    "tauri:build": "bun run --cwd apps/lush tauri:build"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "dependencies": {
    "@lush/config": "workspace:*"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260626.1",
    "dotenv": "^17.4.2"
  }
}
